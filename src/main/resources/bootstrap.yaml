spring:
  application:
    name: cc-rule-engine
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
      config:
        server-addr: http://localhost:8848
        file-extension: yml
        config-long-poll-timeout: 30000